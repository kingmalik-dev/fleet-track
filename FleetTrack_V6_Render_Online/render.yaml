services:
  - type: web
    name: fleettrack
    runtime: node
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: fleettrack-db
          property: connectionString
databases:
  - name: fleettrack-db
